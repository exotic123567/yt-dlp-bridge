# yt-dlp-bridge
A lightweight Brave Chrome extension that connects to a local yt-dlp installation using native messaging. It lets users download videos and audio directly from the browser with selectable formats, quality, and output paths, while running fully offline without any external servers or tracking.
